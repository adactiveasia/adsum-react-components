// @flow

import * as React from 'react';
// import { connect } from 'react-redux';
// import { PopUpDetailActions } from '..';

/**
 * PopUpDetail widget, display a carousel of medias (images or videos) or "Touch to Navigate" message
 * @memberof module:PopUpDetail
 * @class
 * @extends React.Component
 */

// PopUpDetail -> show at POSITION (TOP, RIGHT, BOTTOM, LEFT), WIDTH, HEIGHT with Z-INDEX 15
// Content -> (IN CONSIDERATION WHETHER INCLUDED HERE OR NOT)

//ownprops: position(object{top,right,bottom,left})
//          width, height (IN CONSIDERATION)
//          children (CONTENT)

export default class PopUpDetail extends React.Component<PropsType, StateType> {
    static defaultProps = {

    }

    render(){
        return(

        )
    }
}
